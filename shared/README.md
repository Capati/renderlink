# Shared Folder

This directory contains utility code that is reused across multiple packages. It acts as a common
dependency layer, providing shared functionality required by the main packages or by the user.
