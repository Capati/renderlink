package renderlink

// Relative path to the engine's default asset directory
ASSETS_DIR :: "assets/"

// Default texture to load
TEXTURE_DEFAULT :: #load(ASSETS_DIR + "1px.png")

// Default engine logo texture
// TEXTURE_RENDERLINK :: #load(ASSETS_DIR + "renderlink.png")
